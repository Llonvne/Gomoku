import board.PlayerType;
import board.PointType;
import boardx.BoardX;
import plugins.essentialX.event.GetEvent;
import plugins.essentialX.event.GetEventArgs;

public class Main {
    public static void main(String[] args) {
        var a = new BoardX(5);
    }
}
